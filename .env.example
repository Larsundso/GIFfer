# Internals
Token="Bot <TOKEN>"         # Bot Token

# SSH Upload Configuration
SSH_HOST="your-ssh-host.com"       # SSH server hostname (e.g., cdn.example.com)
SSH_USER="your-ssh-username"       # SSH username for the server
REMOTE_PATH="/path/to/upload/dir"  # Remote directory path where files will be uploaded
CDN_BASE_URL="https://cdn.example.com/files" # Base URL for accessing uploaded files

# ILoveIMG API Configuration (Optional - for GIF compression)
ILOVEIMG_API_KEY=""                # ILoveIMG public API key
ILOVEIMG_API_SECRET=""              # ILoveIMG secret API key
